# Railway에서 PostgreSQL을 추가하면 자동으로 주입됩니다.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# 관리자 계정 생성 시에만 사용합니다 (npm run create-admin).
# 계정을 만든 뒤에는 Railway 변수에서 지워도 됩니다.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
